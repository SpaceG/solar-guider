# Simple Solar Guider — Python dependencies
# Install with: pip install -r requirements.txt
opencv-python
numpy
pyserial
PyQt6
# Nur Windows: Zugriff auf den ASCOM-Treiber (COM) der Montierung
pywin32; sys_platform == "win32"
